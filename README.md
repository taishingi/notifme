# notifme
A simple rust lib to send notify-send notfification

```rust
Notification::new()
            .summary("I am the summary")
            .body("i am the body")
            .icon("spot_dwl")
            .send()
```

